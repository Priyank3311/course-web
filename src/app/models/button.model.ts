export interface ButtonControlModel {
  type: string;
  buttonText: string;
  color: string;
  class?: string;
  matStrokedButton: boolean;
  displayIcon?: boolean;
  iconPath?: string;
  disabledIconPath?: string;
  alt?: string;
}